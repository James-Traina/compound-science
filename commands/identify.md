---
name: identify
description: "[Deprecated v0.5] Use the identification-critic agent or identification-proofs skill directly."
---

/identify has moved. Use the `identification-critic` agent for evaluating identification arguments, or the `identification-proofs` skill for building formal identification arguments (target parameter → model → rank conditions → regularity conditions → estimation link).
